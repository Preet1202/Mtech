steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'europe-west2-docker.pkg.dev/fair-melody-396111/mtech/my-app:${SHORT_SHA}','.']
  # Docker push to Google Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-west2-docker.pkg.dev/fair-melody-396111/mtech/my-app: +{SHORT_SHA}']
# Store images in Google Artifact Registry 
images:
  - europe-west2-docker.pkg.dev/fair-melody-396111/mtech/my-app:${SHORT_SHA}
